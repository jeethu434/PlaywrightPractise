# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: practise.spec.js >> Practise
- Location: tests\practise.spec.js:3:5

# Error details

```
Error: locator.check: Target page, context or browser has been closed
Call log:
  - waiting for getByRole('checkbox', { name: 'checkBoxOption1' })

```

# Test source

```ts
  1  | import { expect, test } from '@playwright/test'
  2  | 
  3  | test("Practise", async ({ page }) => {
  4  |     await page.goto("https://rahulshettyacademy.com/AutomationPractice/");
  5  |     const lsitLocator = page.locator('//div[@class="tableFixHead"]//table[@id="product"]//tr');
  6  |     const count = await lsitLocator.count();
  7  |     const Amounts = [];
  8  |     for (let i = 1; i < count; i++) {
  9  |         Amounts.push(Number(await lsitLocator.nth(i).locator('//td').last().textContent()));
  10 |     }
  11 |     console.log(Amounts);
  12 |     const amount = Amounts.reduce((pn,cn)=>pn+cn,0);
  13 |      expect(await page.locator(".totalAmount").textContent()).toContain(String(amount));
  14 |      await page.pause();
  15 | 
> 16 |      await page.getByRole('checkbox',{name:'checkBoxOption1'}).check();
     |                                                                ^ Error: locator.check: Target page, context or browser has been closed
  17 | })
  18 | 
```